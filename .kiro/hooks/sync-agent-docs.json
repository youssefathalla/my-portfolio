{
  "version": "v1",
  "hooks": [
    {
      "name": "Mirror .kiro agent docs into .agents",
      "trigger": "PostFileSave",
      "description": "Whenever a steering file or skill under .kiro/ is saved, mirror it into .agents/ so Kiro and the other agents never drift. Bodies are synced; each tool keeps its own front matter.",
      "matcher": "\\.kiro[\\\\/](steering|skills)[\\\\/].*",
      "action": {
        "type": "command",
        "command": "npm run sync:agents --silent",
        "timeout": 60
      }
    }
  ]
}