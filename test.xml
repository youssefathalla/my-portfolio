Refactor the following component to use modern Signals and clean architecture according to our team's guidelines and schema.

<coding_guidelines>
- Use fine-grained reactivity with Signals.
- Enforce strict type validation.
- Do not mutate state directly; return immutably updated objects.
</coding_guidelines>

<data_schema>
interface UserProfile {
  id: string;
  name: string;
  role: 'admin' | 'member';
  isActive: boolean;
}
</data_schema>

<legacy_code>
class ProfileManager {
  private user: any;

  setUser(u: any) {
    this.user = u;
  }

  toggleStatus() {
    this.user.isActive = !this.user.isActive;
  }
}
</legacy_code>

<instructions>
1. Convert `ProfileManager` to use signal primitives.
2. Ensure full type-safety matching `UserProfile`.
3. Output only the refactored TypeScript code without unnecessary commentary.
</instructions>